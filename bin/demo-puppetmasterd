#! /bin/bash
set -u
set -u

base="/demp/puppet-demotools"
export PATH="${base}/bin:${PATH}"

exec puppetmasterd --no-daemonize --verbose --modulepath="${base}"/modules:/etc/puppet/modules:/usr/share/modules $@
